[build-system]
requires = ["setuptools", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "uhfreader"
version = "0.1.0"
description = "UHF Reader desktop app (pywebview)"
requires-python = ">=3.9"

[tool.pyinstaller]
# kept for documentation; we call CLI directly in script
